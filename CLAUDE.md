# chicago (chicago.polecat.live) — agent guide

4D Chicago: a year-parameterized, walkable, source-cited reconstruction of early
Chicago. **Read `chicago/4d/AGENTS.md` every session** — it is the project's contract
(provenance rules, the 1835 / Indigenous-history standing constraint, the
visible-progress rule, the What's-New length budget). `README.md` has the URL map.

## Where the work lives

- The 4D project is `chicago/4d/` (paths inside it did not change when this repo was
  split out of kevinrhaas/custom on 2026-09-23). Run tools from there.
- **Tickets are a separate repo, kevinrhaas/chicago-tickets**, cloned into
  `chicago/4d/tickets/` (gitignored) by `bash chicago/4d/tools/tickets.sh`.
  `node tools/ticket.mjs …` commits and pushes ticket changes straight to that repo's
  `main` — never put a ticket file or QUEUE.md in a code PR. `claim` before work;
  `done --pr N` sets `review` and the tickets repo's settle workflow marks it done
  when the PR merges. A question only the owner can answer: `ticket.mjs ask T-NNNN
  --question … --option a=… --option b=… --rec a --why …` — the ticket stays in the
  queue with its question, and runs skip it until he answers.
- `site/4d/` is GENERATED by `chicago/4d/tools/publish.sh` and untracked.

## Non-negotiables

- **Gate before every commit**: `cd chicago/4d && ./tools/check.sh` (read its last
  lines — tagged `self-test |` FAIL lines are meant to fire), then the smoke by parts
  (`node tools/smoke_budget.mjs --for-diff`). Mobile (390×780) is a release gate.
- **Pipeline**: branch from `dev`, PR into `dev`, merge on a green gate. Production
  (`main`) moves only when the owner dispatches `chicago-4d-promote-to-prod.yml`.
  `deploy.yml` is the single deploy authority (main at `/`, dev preview at `/4d/dev/`).
- **Changelog contract**: `chicago/4d/renderers/web/js/changelog.js`, fleet format,
  new entry on TOP with `v: null, ts: ''`; stamp with
  `node chicago/4d/tools/stamp-changelog.mjs`, verify with `check-changelog.mjs`.
  Published at `chicago.polecat.live/4d/js/changelog.js`, which Manager and the
  polecat.live launcher parse — never break it, never move it.
- **Provenance is the product.** Never invent a source; `reconstructed` is a tier to
  build at, recorded in `docs/LIBERTIES.md`, never a claim of evidence.
- No model identifiers in repo artifacts.
